#ifndef UTILS_H
#define UTILS_H

#include <ESPAsyncWebServer.h>
#include <DNSServer.h>

extern AsyncWebServer server;  // Declaration of the server
extern DNSServer dns;          // Declaration of the DNS server

#endif // UTILS_H
