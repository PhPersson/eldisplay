

// char api_url[45] = "";  // API-url to fetch the price
// char electricityPriceArea[4] = ""; // Any of SE1, SE2, SE3 or SE4
// float priceThreshold = 0.30; // The threshold when price should be consider high
// bool shouldAddTax = true; // false if not include tax, only tariff