

// char api_url[45] = "";  // API-url to fetch the price
// char priceArea[4] = ""; // Any of SE1, SE2, SE3 or SE4
// float threshold = 0.30; // The threshold when price should be consider high
// bool addTax = true; // false if not include tax, only tariff